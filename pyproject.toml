[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "adaptive-tts"
version = "0.1.0"
description = "Agentic learned budget allocation for test-time scaling"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*"]
